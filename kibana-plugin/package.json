{
  "name": "scorestack",
  "version": "0.8.2",
  "private": true,
  "scripts": {
    "plugin-helpers": "node ../../scripts/plugin_helpers",
    "kbn": "node ../../scripts/kbn",
    "start": "cd ../../ && node scripts/kibana --dev --no-base-path -c plugins/scorestack/kibana.dev.yml",
    "build": "yarn plugin-helpers build --kibana-version 8.1.2",
    "lint": "eslint --ignore-path .gitignore --color .",
    "lint-fix": "eslint --ignore-path .gitignore --color --fix ."
  },
  "devDependencies": {
    "@elastic/eslint-config-kibana": "link:../../packages/elastic-eslint-config-kibana",
    "babel-eslint": "^10.1.0",
    "eslint": "^8.12.0"
  }
}