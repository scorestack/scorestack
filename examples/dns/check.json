{
    "id": "dns-example",
    "name": "Example dns check",
    "type": "dns",
    "group": "example",
    "score_weight": 1,
    "definition": {
        "Server": "{{.Server}}",
        "Port": "{{.Port}}",
        "Fqdn": "{{.Fqdn}}",
        "ExpectedIP": "{{.ExpectedIP}}"
    }
}