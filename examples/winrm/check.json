{
    "id": "winrm",
    "name": "WinRM",
    "type": "winrm",
    "group": "example",
    "score_weight": 1,
    "definition": {
        "Host": "{{.Host}}",
        "Port": "{{.Port}}",
        "Username": "{{.Username}}",
        "Password": "{{.Password}}",
        "Cmd": "{{.Cmd}}",
        "Encrypted": "{{.Encrypted}}",
        "MatchContent": "{{.MatchContent}}",
        "ContentRegex": "{{.ContentRegex}}"
    }
}