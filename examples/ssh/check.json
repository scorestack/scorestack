{
    "id": "ssh",
    "name": "SSH",
    "type": "ssh",
    "group": "example",
    "score_weight": 1,
    "definition": {
        "Host": "{{.Host}}",
        "Port": "{{.Port}}",
        "Username": "{{.Username}}",
        "Password": "{{.Password}}",
        "Cmd": "{{.Cmd}}",
        "MatchContent": "{{.MatchContent}}",
        "ContentRegex": "{{.ContentRegex}}"
    }
}