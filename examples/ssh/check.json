{
    "id": "ssh-example",
    "name": "Example SSH Check",
    "type": "ssh",
    "group": "example",
    "score_weight": 1,
    "definition": {
        "Host": "{{.Host}}",
        "Port": "{{.Port}}",
        "Username": "{{.Username}}",
        "Password": "{{.Password}}",
        "Cmd": "{{.Cmd}}",
        "MatchContent": true,
        "ContentRegex": "{{.ContentRegex}}"
    }
}