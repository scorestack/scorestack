{{#models}}{{#model}}import { {{classname}}Schema } from './{{classFilename}}';
export * from './{{classFilename}}';{{/model}}
{{/models}}

export const schemaMap = {
    {{#models}}{{#model}}'{{name}}': {{classname}}Schema,
    {{/model}}{{/models}}
}

import { Protocol } from './protocol';

export function schemaFromProtocol(protocol: Protocol): string {
    return schemaMap[Protocol[protocol].toLowerCase()];
}